# Nova Act Samples Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
nova-act>=1.0.0
pydantic>=2.0.0

# For concurrent processing
# (concurrent.futures is part of Python standard library)

# Optional: For enhanced data processing
pandas>=1.5.0
numpy>=1.20.0

# Optional: For data visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Optional: For advanced text processing
nltk>=3.8.0
textblob>=0.17.0

# Optional: For working with dates
python-dateutil>=2.8.0

# Optional: For HTTP requests (if needed for API calls)
requests>=2.28.0

# Optional: For JSON schema validation
jsonschema>=4.0.0

# Optional: For CSV/Excel file handling
openpyxl>=3.0.0

# Optional: For environment variable management
python-dotenv>=0.19.0
